use std::fs;

//INFO: Default includes: use log::*;use anyhow::{Result,bail,Context};
use anyhow::{Context, Result, bail};
use log::*;

fn main() {
    colog::init();
    info!("init colog");



const FILE_PATH  = 
    fs::read(path)
}
